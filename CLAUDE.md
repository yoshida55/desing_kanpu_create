# CLAUDE.md — デザイン参照ストック & 曖昧検索システム

このファイルは、このプロジェクトで作業する Claude（と人間）が最初に読む案内図。
**何を・なぜ作っているか／今どこまで出来ているか／次に何をするか**をここに集約する。

最終更新: 2026-07-09

---

## 0.1 コーディング仕様書の書き出し（2026-07-09・新機能）

カンプを「コーディング担当にそのまま渡せる仕様書」に起こす機能（`src/spec.py`・AIなし＝無料）。
- 編集バーの「📐 仕様書を作る」→ 未保存の編集を焼き込み→Playwrightでカンプを実測
  （getBoundingClientRect/getComputedStyle）→ `data/camps/specs/<元名>_spec.html` に保存して別タブで開く。
- 中身：全体スクショをセクションごとに切り出し＋赤ピン番号＋表（サイズ・文字・色/背景・
  余白/角丸・動き・メモ）。動きは機械判定（`reveal`/`fxa_*`/`data-cedelay`/独自keyframes→
  アニメ停止直前に`data-specanim`へ退避して読む。停止後に読むと消えているので順序を変えないこと）。
- 仕様書自体も編集できる：ツール経由（`/spec/<file>`）で開けばセルを直接書き換え→💾保存
  （`/api/save_spec_html`・カンプと同じDOM丸ごと保存方式）。file://直開きだと保存不可（案内が出る）。
- API: `/api/make_spec`(POST・非同期・同時1つ) / `/api/make_spec/status` / `/spec/<file>`。
- specs/ はcamps直下と分けてあるので履歴一覧（`camp_*`グロブ）には混ざらない。Git同期はされる。

## 0.2 直近の大改修（2026-07-07後半）＝生成品質の底上げ（Fable 5で集中実施）

Fable 5（サブスク最終日）に「モデルのデザイン知識をツール側に移す」作業を集中実施。
以後どのエンジン（GPT/Haiku/DeepSeek）で生成・修正しても効き続ける資産。

### ① 生成プロンプトの全面強化（`camp.py` ③生成指示）
- 追加した柱：**タイポグラフィ（文字階層4段制・日本語行間1.9〜2.1・1行38字制限）／
  余白の設計（8px倍数・近接の原則・見出し上下2:1）／色の比率70:25:5＋背景の白⇔薄色リズム／
  コピーの質（抽象スローガン禁止・具体的な数字と固有名詞）／出力前セルフチェック6項目**。
- AIっぽさ禁止も拡充（紫グラデ・同構造3連続・全カード同一サイズ等）。
- 効果は同条件Before/Afterで実測：clamp 4→11箇所、フッターリンク0→6本、施設名等の固有名詞入りコピー化。

### ② 修正系プロンプトにも同じ規律を焼き込み
- `_EDIT_SYSTEM`（右クリック修正）・`_SUGGEST_SYSTEM`（改善案）・`STYLE_INS`（✨おしゃれ化）に
  要点だけ圧縮して追加（修正は毎回課金されるので生成用より短く）。
- ★同日さらに`STYLE_INS`を全面書き換え→`styleIns()`関数化（viewer.py）：
  1) 「均等カード化禁止」と書くだけでは修正AIが「角丸＋影＋等間隔」の箱揃えに逃げる（実使用で頻発）
  2) 代替4型をリストで並べてAIに選ばせると**先頭の非対称グリッドばかり選ぶ**（これも実使用で頻発）
  → 対策：ボタンを押すたびに**JS側でランダムに1型だけ**（非対称グリッド/ずらし配置/互い違い/
  横帯リスト）を指定する方式に。気に入らなければ押し直せば別の型が出る。
  さらに**重み付き抽選＋前回と同じ型は連続で選ばない**（localStorage `__ce_style_last`）。
  「箱が並ぶ」見た目が残る型（非対称w2/ずらしw1）より箱をやめる型（互い違いw3/横帯w3）を
  出やすくした（ユーザー要望「3つ箱はやめてほしい」）。出力前セルフチェック4項目
  （テキスト全残し/画像全残し/コントラスト/空の色面禁止）も追加済み。
- ★★最終形＝**テキスト保全ゲート**（camp.py・プロンプトで頼む方式の限界への根本対策）：
  「中身を保て」といくら指示してもGPT/Haiku問わず説明文を確率的に落とす
  （実例：3項目全部の説明文を捨てて同じ汎用文を3回繰り返した）。
  → AIを信じず**Pythonで機械照合**：差し替え前に元セクションの全テキスト断片（8文字以上・
  空白除去で正規化）と全img srcを抽出→AI出力と照合→欠落があれば「この文が消えている」と
  名指しで1回自動リトライ→それでも欠落なら**差し替え中止**（壊れた版を保存しない）。
  `_content_losses()`/`edit_camp_section(keep_text=True)`。✨おしゃれ化だけがこのゲートを
  通る（`/api/edit_camp`の`keep_text`フラグ）＝「文言を変えて」等の正当な編集は通らない。
  テスト済み：実際の事故パターンで3欠落を検出・正常な組み替えは誤検出0。
- ★ヒーロー専用の型（`HERO_TYPES`）：トップ（1番目のセクション）のおしゃれ化は
  リスト4型ではなく**縦書きヒーロー／コラージュヒーロー**から抽選。
  型の中身は想像ではなく、**ユーザーが良例として挙げた過去カンプ2本の実CSSから数値採取**
  （camp_20260702_223653=青コラージュ：正方形3枚をmargin-bottom:-46pxで大写真に差し込む・
  縦書き56px・11px英字2行組ラベル／camp_20260703_003012=るわみ：縦書き見出しを半透明紙カード
  rgba(255,255,255,.85)に載せる・ピル型バッジ・背景に白薄の円/枠を浮遊）。
  数値入りの言葉レシピなのでスクショを渡せないテキスト専用エンジンでも再現できる。
- ★型のページ内重複防止（`data-cestyle`刻印）：おしゃれ化は1セクションずつ実行され
  「ページの他セクションがどんな型になったか知らない」ため、複数セクションに使うと
  ページ全体が同じ表情（全部01/02/03番号付き等）になる問題があった。
  → 成功時にサーバーが新セクションのルートに`data-cestyle="型名"`を機械で刻印
  （AI任せにしない・`edit_camp_section(style_type=)`）。次回のおしゃれ化はページ内の
  刻印を全部読んで**使用済みの型を抽選から除外**＋AIにも「番号あしらい使用済み」と通知。
  HTMLに刻むので保存・別バージョン・家↔会社同期でも引き継がれる。
- ★おしゃれ化はセクション専用：右クリック位置がセクション外（ヘッダー等）で
  sIdx=-1になった場合、ページ全体編集（高額）に落とさずその場で中止するガードを追加。
  「箱を揃えるだけの修正は禁止」を明文化し、セクション内のHTML組み替えも許可した。

### ③ 業種別デザインレシピ（`src/recipes.py`・新規）
- 14業種（介護/就労支援/医療/士業/飲食/美容/建設住宅/保育/塾/IT・SaaS/採用/NPO/宿泊/EC）の
  「誰に何を伝えるか・配色・必須セクション・写真の選び方・コピーの型・禁じ手」をデータ化。
- briefの文中キーワードから自動で1件だけマッチ→生成プロンプトに注入（`recipe_prompt_block`）。
  マッチテストは10/10。該当なしなら何も足さない。レシピは追加自由だが1件10行前後を保つこと。

### ④ ベース×アニメの相性判定（mix & match の弱点解消）
- 保存済み**画像ベクトルの内積**で2サイトの雰囲気の近さを算出（`camp.pair_fit_score`・無料で一瞬）。
- しきい値`_FIT_NEAR=0.635`/`_FIT_FAR=0.56`は**手持ち15サイトの全ペア実測分布**（min0.46/中央0.60/max0.73）
  から校正（決め打ち禁止ルール準拠）。ストックが増えたら見直す。
- 生成時：相性に応じた「動きの翻訳」指示を自動注入（遠いほど控えめに翻訳・Aの世界観優先）。
- UI：ベース＋アニメを**選んだ瞬間**にヒント行へ「相性◎/○/△ 数値」を表示
  （`/api/pair_fit`＋pickerの`onPick`）。ジョブ進捗のphaseにも同じ一言が付く。

### ⑤ 保存・配信まわりのバグ修正3件（総点検で発見）
1. **保存版の保険（`_REVIEW_FALLBACK`）に隠しメニューガードが無かった**：
   保存ファイルをダブルクリック直開き→2.5秒後にメガメニュー等が強制表示されて開きっぱなしになる穴。
   配信版（`_SERVE_SAFETY`）と同じ`inOverlay`判定を追加。既存ファイルも配信時の差し替えで自然治癒。
2. **`_SERVE_SAFETY`のフォールバックが🖍マーカー(fxa_hl)を知らなかった**：
   本家FX_RUN不在時にマーカーが`--hlw:0`のまま＝一生引かれない。`--hlw:100`で少なくとも表示するよう修正。
3. **保存が直接上書きだった**：書き込み途中のクラッシュでファイル半壊のリスク→
   一時ファイル(.html.tmp)→`os.replace`の安全方式に変更（`api_save_camp_html`）。
- 既知の限界（未修正・報告のみ）：セクション編集は正規表現ベースなので、
  **`<section>`の入れ子**があると範囲がズレる。今の生成カンプは入れ子を作らないので実害なし。
  「3番を直せと言ったのに別の所が変わる」が起きたらこれが犯人。

### ⑤-2 同日夜の追加分（Fable最終日の駆け込み実装・全部テスト済み）
- **業種レシピ 14→20業種**（`recipes.py`）：整体/フィットネス/ペット/葬祭/自動車/製造BtoB。
  各レシピにコピー実例文を焼き込み（Fableの文章を資産化）。マッチは**一致キーワードの合計文字数**方式に
  変更（「動物病院」が医療の「病院」に誤マッチする衝突を解消）。テスト15/15。
- **ハズレ回アラート**（`src/quality.py`・新規）：生成完了ごとに自動チェックし、薄い出力
  （本文2000字未満 or 8セクション未満）なら進捗欄にオレンジ警告カードを残す（✕で閉じる）。
  ★開発メモ：最初ピクセル空白率で作ったが「良デザインの意図した余白」と区別できず不採用。
  良悪を分離したのは情報量（文字数/セクション数）。しきい値は実カンプ6本（良3悪3）から校正。
- **◎○△✖評価システム**：カンプ画面右上（編集バーのヘッダ）に評価ボタン。同じのを再度押すと解除。
  `data/camps/_quality_log.json` に「手本×モデル×仕上がり×評価」が貯まり、
  手本を選んだ瞬間にヒント行へ「この手本の過去実績: ◎2・✖1」（3件以上でハズレ率50%↑なら⚠）。
  API: `/api/camp_rate`(GET/POST) `/api/base_stats`。狙い＝ユーザー評価を教師データに手本選びを賢くする。
- **生成プロンプト修正**（`camp.py`）：均等カード並びは**ページ2回まで**（同じ見た目の繰り返し禁止・
  手本にあるなら手本優先）＋代替4型（互い違い/非対称グリッド/横帯リスト/ずらし重ね）を提示。
  「薄いセクションをmin-height:100vhに引き伸ばさない」「.container等の幅クラスを子のwidth:100%で
  上書きしない」も禁じ手に追加（後者は実際に起きた左端張り付きバグの再発防止）。

### ⑥ その他
- mix & match の初実戦テスト成功（本屋×ゆるアニメ→就労支援、建設×採用→デイサービス。
  手本の独自keyframes・配色が数値レベルで乗ることを確認）。
- 全15サイトのデザイントークン取得完了（stripe/sunfluoroの不足分を再取得）。
- ⚠運用注意：サーバーは必ずvenvで起動（`起動.bat`推奨）。システムPythonだと
  SDKバージョン違いで修正系が`Client.__init__() got an unexpected keyword argument 'proxies'`で落ちる。

---

## 0. 直近の大改修（2026-07-02）＝カンプ「その場編集」とマルチエンジン

この日、カンプ生成後の**「見ながらその場で直す」体験**と**エンジンの選択肢**を大きく強化した。
以下はすべて実装済み。詳細は各コード（`camp.py` / `viewer.py` の `_EDIT_BAR` / `config.py`）。

### ① マルチエンジン（生成／修正を別々に選べる）
- 生成／修正で**別エンジン**を指定できる（`HtmlGenConfig.provider` = 生成、`edit_provider` = 修正）。
  - `.env`：`DESIGN_STOCK_HTML_PROVIDER` / `DESIGN_STOCK_EDIT_PROVIDER`。
- 対応プロバイダ：**Claude / GPT(OpenAI) / Gemini / DeepSeek**。`camp._call_llm(system, content, provider)` が出し分け。
  - `_call_anthropic` / `_call_openai` / `_call_gemini`(REST) / `_call_deepseek`(OpenAI互換・base_url差し替え)。
- **モデルもドロップダウンで選ぶ**（⚙設定）。選んだ瞬間に自動保存（ラジオもモデルも）。
  - Claude：**Haiku 4.5(修正に最適/激安 $1/$5)** / Opus 4.8 / Sonnet 5 / Sonnet 4.6。
  - GPT：gpt-5.5 / gpt-5.4。Gemini：3.1 flash-lite / flash。
  - **DeepSeek：V4-Pro($0.44/$0.87・Haiku超えの精度)** / V4-Flash(最安 $0.14/$0.28)。`DEEPSEEK_API_KEY`。
- **接続テスト**は Claude/GPT/DeepSeek＋Gemini をまとめて検証（`_test_all`/`_test_key`/`_test_gemini`/`_test_deepseek`）。
- キー未設定の判定は用途別：`_gen_ready()`（生成）/ `_edit_ready()`（修正）/ `_provider_ready(provider)`。

### ② コスト設計（破産回避）★重要
- **料金は「出力トークン」が主。全文を返す＝高い**。
  - **セクション修正（右クリック or ①で選択）＝数円**。**ページ全体を直す＝数十〜100円**（全文往復＋thinking）。
- `submit()` は section=-1（ページ全体）のとき**確認ダイアログで警告**（うっかり高額を防ぐ）。
- LLM呼び出しに**制限時間**（Claude/OpenAI=180s・Gemini/DeepSeek=150/180s）→「永遠に続く」を根絶。
- **おすすめ運用**：生成=GPT/Claude（画像手本が要る）／修正=**DeepSeek V4-Pro か Haiku 4.5**／説明=Gemini。
- ⚠ **DeepSeekは画像を送らない実装**（テキストのみ）。修正はテキストだけなので最適。**生成にDeepSeekを使うと手本スクショを渡せず"らしさ"が落ちる**。

### ③ その場編集バー（`_EDIT_BAR` を `/camp/<file>` で注入）
- **右クリック**した要素に直接：アニメ／背景装飾／自由指示／改善案を出せる。
  - `pickTarget()`：1文字ずつ分割spanなどインライン断片を掴んだら、**見出し等のまとまりまで親を上る**。
  - 選択マーカーは**青い点線**（`.__ce_sel`）＝デザインの枠と見分けやすく、外クリック/読込で消える。
- **プリセット**（`PRESETS`）：アニメ10種＋**背景装飾6種**（ドット/ストライプ/方眼/幾何学/紙質感/水彩にじみ）。
  - 背景プリセットは `bg:1` フラグ付き。**画像を右クリック時は「画像の周りだけ」／余白を右クリック時は「セクション全体」**に出し分け。
- **画像は右クリックで2択**：🖼差し替え（AIなし・一瞬 `swap_image`）／🎨背後に画像を敷く（水彩など・`openBgPicker`→AI）。
  - 画像のAI指示は不安定なので、差し替え/背景敷きを優先提示。
- **進捗トースト**（画面下・経過秒＋段階表示）。編集は非同期ジョブ（複数同時OK）。
- **⭐名前を付けて保存**：良い版に名前を付けてお気に入り登録（`camp.set_camp_name`→`data/camps/_names.json`）。
  - HTML本体は汚さず別JSONに記録。履歴一覧で名前付きは上にオレンジ＋⭐表示、✎で改名。

### ④ 「消える(真っ黒)」対策＝全部見える保険
- 出現アニメの `opacity:0` がトリガー不発で残ると真っ黒に。→ **保険を二重化**：
  - 生成/修正時：`_REVIEW_FALLBACK`（`_SAFE_START/END`で挟む・`_finalize_html`が古い版を剥がして最新を入れ直す）。
  - **配信時**：`_SERVE_SAFETY`（`_inject_edit_bar`で全カンプに注入）→**古い壊れたファイルも見た瞬間に直る**。
  - どちらも「クラス名に関係なく、透明/非表示の要素を2.2〜2.5秒後に強制表示」する。
- セクション修正も必ず `_finalize_html` を通す（差し替え後に保険が効く）。

### ⑤ 効いたバグ修正
- **ThinkingBlockエラー**：Sonnet 5 / Opus 4.8 は考える過程(ThinkingBlock)も返す。
  `content[0].text` 決め打ちで落ちていた → `_anthropic_text(msg)` で **type=='text' のブロックだけ結合**（`vibe.py`も同様）。
- **Geminiの説明文混入**：`_strip_fragment` を強化（```フェンス除去＋最初の`<`〜最後の`>`だけ採用）。

### ⑥ AIなしの直接編集＝見ながら手で調整（2026-07-02 後半で大幅追加）
LLMを介さず、右クリックした要素を**その場で直接いじる**系を `_EDIT_BAR` に増設。速い・無料・確実。
- **右クリックで即ドラッグON**（`setDragOn`）＝基本は移動。掴んで動かせる。解除は「✋ドラッグ中」ボタン。
- **位置**＝矢印ナッジ／ドラッグ、**大きさ**＝拡大縮小＋横だけ/縦だけ（`scaleX/scaleY`）、**角度**＝左右回転（`rotateBy`）。
  - すべて `transform` を要素の inline style に `!important` で当てる（`applyTf`）。元の変形(回転など)は `data-cebt` に退避。
    ★CSSクラス由来の斜め配置は `getComputedStyle` の matrix を拾って保持（動かすと真っ直ぐになる不具合を防止）。
- **保存は1ボタンに統一**：動かすとヘッダの保存ボタンが「💾 変更を保存」に変化（`markDirty`）。
  - ★重要：保存はサーバーで元ファイルを読み直さず、**ブラウザの現DOMから編集UIを除いて丸ごと保存**（`cleanHtml`→`/api/save_camp_html`）。
    だから位置・角度・画像差替が一括で焼き込まれ「保存しても戻る」が起きない。`data-cetx`等が焼ければ保存成功の証拠。
- **画像差し替えもブラウザ側**（`openPicker`→`saveLayout`）：他の編集ごと保存＝角度が戻らない。
  - 重なり画像は**右クリック座標に重なる候補を集めて選ばせる**（`elementsFromPoint`＋座標に矩形が重なる要素の走査で `pointer-events:none` の装飾も拾う）。**`<img>` も背景画像(`background-image`)も対象**（`pickWhichImg`／cand=`{el,type:'img'|'bg',url}`）。
  - ⚠ **疑似要素(`::before/::after`)の装飾・gradientは掴めない**（DOMに実体が無い）。→ 変えたいならAI指示、または「div要素に作り替えて」と依頼。
- **▶ 動きプレビュー**（`playAnim`／8種・保存しない）。
  - ★地雷：**CSSアニメも Web Animations(`el.animate`)もこの環境では効かなかった**（一瞬で終わる/無反応）。→ **`requestAnimationFrame`で毎フレーム手動描画**に落として確実化（`ANDEF`）。ループ系はメニュー閉じ/別要素選択で停止（`stopAnim`）。
- **メニュー(`#__ce_cm`)自体もドラッグ移動**でき、位置を **localStorage に記憶**（`lastMenuPos`）→再読込しても同じ場所に出る。
- **✨ このセクションをおしゃれに**（`STYLE_INS`）＝AIが中身を保ったまま一括ブラッシュアップ。使うAIは**編集バーのドロップダウンで即切替**（`#__ce_ai`→`/api/settings`の`edit_provider`）。おしゃれ化は見た目判断なので Claude/GPT 推奨。
- **接続テストは修正エンジンも検証**（`_test_all`が生成＋修正(edit_provider)＋Geminiを並べる）。
- **履歴**は開いたら先頭（最新）が見える位置へスクロール（`loadCampHistory`）。

---

## 0.5 直近の大改修（2026-07-05）＝カンプの「演出タイミング」を作り込み＋バグ退治

この日は、右クリック編集バーの**アニメ/マーカー系のかゆい所**を直しつつ、
**「要素をどの順番・何秒後に出すか」を自由に指定できる仕組み**（`data-cedelay`）を追加した。
あわせて、保存・配信まわりに潜んでいた地味なバグを何本も退治した（詳細は7章にも記録）。

### ① 右クリック＝即ドラッグONを廃止（文字選択との衝突を解消）
- 右クリックすると自動でドラッグモードになる仕様だったが、これが**文字をドラッグ選択して
  下線/マーカー/文字色を付ける機能と衝突**していた（選択のmousedownがドラッグに奪われる）。
- 右クリックだけでは動かせないようにし、「🖱 掴んで動かす」ボタン（メニュー最上部に移動）／
  **Altキー+ドラッグ**（新規・ボタンを押さず即つかめる）で動かす方式に統一。
- ★2026-07-06に再反転：Altを毎回押すのが手間という声を受け、**普通にクリック＋ドラッグ＝即つかんで動かす**を既定に戻した。
  代わりに「文字を選んで下線/マーカー/文字色を付ける」操作だけを**Alt+ドラッグ**に割り当てた
  （Alt無しだとドラッグが選択より先に割り込むため）。`html.__ce_altmode`のカーソルも`move`→`text`に変更
  （Alt中＝選択モードの合図）。

### ② 🖍マーカーの太さ・速さを調整可能に＋再生方式を作り直し
- 選択ポップアップ／右クリックメニューに「太さ」「速さ」ボタンを追加（`fxHlThick`/`fxHlSpeed`）。
- ★CSSの`transition`を付け外しして再生し直す方式は環境によって不安定 →
  **`--hlw`(0〜100)をrAFで毎フレーム手動更新する方式**に変更（`sweepHl`／`window.__fxaSweepHl`）。
  連打対策に世代番号(`__hlGen`)で古いループを自動停止。
- 帯の位置もデフォルトを文字の下寄り（70%〜92%）に調整。

### ③ 「演出の順番づけ」＝`data-cedelay`を新設
- 要素に`data-cedelay="ミリ秒"`を付けると、その要素の出現(`fxa_pre`)またはマーカー(`fxa_hl`)が
  指定時間だけ待ってから再生される。
- ボタン式の「グループ(`data-cegrp`)」機能も先に作ったが、微調整が難しく
  **会話でミリ秒を直接指定する運用**に落ち着いた（`.claude/skills/カンプ/SKILL.md`の
  「A. 演出の順番づけ」に手順化済み）。
- ⚠**罠**：配信時にfxa_pre要素を強制表示する古い保険（`_SERVE_SAFETY`の`fxaStart`）が
  `data-cedelay`を知らずに先に表示してしまうバグがあった→両方が同じルールに従うよう修正済み。

### ④ 動きを消すボタン／保存まわりの地雷を除去
- 「🚫 この要素の動きを消す」ボタンを追加（`removeBake`）＝焼き込んだ出現/ループ/文字アニメを
  素の状態に戻せる。
- **保存(`cleanHtml`)の抜け穴を2つ発見・修正**：
  1. ブラウザ拡張機能(Glasp)がページに注入したUI断片が保存のたびに紛れ込み増殖
     （1ファイルで708個・223KB→84KB相当の肥大化）→ 保存時に自動除去するよう修正。
  2. マーカーが伸びきった状態(`--hlw:100`)で保存すると、次回開いた時にアニメせず
     「最初から引かれた状態」になる → 保存時に`--hlw`を必ず0へリセットするよう修正。

---

## 1. 目的・背景（なぜ作るのか）

- これまでの業務は「デザインカンプを元に HTML/CSS を実装する」のが主だった。
- 今後は**デザイン工程から関わる**可能性が出てきた。
- そこで、自分が「良い」と感じた Web サイトの **見た目・雰囲気を集めて・探して・再利用できる土台** を作る。
- 最終ゴールは、集めたデザイン資産を使って **デザインカンプの叩き台を素早く生成する** こと。

> 本質は **「デザイン版の RAG（検索して生成に渡す知識ベース）」**。
> 1サイトを丸ごと複製するのではなく、**自分のストックから新しいものを生成する**のが設計思想の核。
> （複製ではなく「参照 → 作り直し」。これが線引き。）

正式な仕様書は [仕様/design-swipe-system-spec.md](仕様/design-swipe-system-spec.md)。迷ったら原典はそちら。

---

## 2. システム全体像（4つの層）

```
① 集める  URL → Playwrightで撮影（firstview/fullpage）＋メタdata → SQLite
② 探す    SigLIP-2で画像/テキストを同じベクトル空間へ → numpy総当たりで曖昧検索
③ 参照     検索結果カードのURLから実物サイトへ飛び、動き・アニメを確認
④ 生成     （将来）検索で引いたセクション＋デザイントークンをAIに渡しカンプHTMLを生成
```

検索の主軸は **「見た目のパターン・雰囲気」** での曖昧検索。
機能名検索（例：「スタッガー演出」）は後付けのタグ層の役割で、初期は扱わない。

---

## 3. 今どこまで出来ているか（現在地・2026-07-01）

✅ **Phase 1**（取り込み・埋め込み・検索）／**Phase 2**（ローカルビューア・モデル常駐）
✅ ビューアだけで一通り完結：**登録・検索・似てる検索・🗑削除**
✅ **雰囲気描写ハイブリッド**（Claudeが雰囲気を言語化→検索に効かせる）
✅ **デザイントークン抽出**（実際の配色・フォント・余白・角丸）→ カードに配色スワッチ
✅ **アニメ録画**（スクロール動画webm）＋ホバー再生、**ライブラリ検出**（GSAP等）
✅ **カンプ生成（Phase 4の核）**：手本サイトのfullpageを骨格に、トークンで配色を寄せ、
   ダミー写真＋スクロールアニメ付きの叩き台HTMLを生成。「🎨ベースに生成」でA主役を指定可
✅ **生成エンジン切替**：Claude(Opus 4.8) / GPT(OpenAI) を**設定画面**で選べる（`.env`に保存）
✅ **詳細ビュー**：カード画像クリックで、全体スクショ＋録画＋配色＋フォント＋ライブラリ＋
   雰囲気＋**抜き出した画像ギャラリー**を1画面に集約
✅ **画像の抜き出し**：ライブページから実画像（img/背景/picture/dataURI）を収集・保存（`assets.py`）

実機検証で「SigLIP-2 が効く」ことは確認済み：
- 「カラフルで手描き風」→ Notion が1位 ／「青のグラデのテック」→ Stripe が1位
- image→image：Apple→Apple = 1.0000（自己一致＝計算が正確）
- カンプ生成：ichizenbooks(青×黄)主役→「ヨムトモ」が10セクションの骨格＋配色を踏襲

---

## 4. 使い方・起動（重要）

### 起動は「起動.bat」をダブルクリック（一番ラク・推奨）
- `起動.bat` をダブルクリック → サーバ起動＋ブラウザで `http://127.0.0.1:5000` が自動で開く。
- ❌ **HTMLファイルを直接ダブルクリックしない**（`file://` で開くと全ボタンが
  「Failed to fetch」になる。ツールは"動くサーバ"なので必ず `http://127.0.0.1:5000` 経由）。
- ✅ 例外：生成した**カンプ**（`data/camps/*.html`）は単体で完結＝ダブルクリックでOK。

### コマンドでも起動できる
```bash
# ビューア起動（このPCはメモリが厳しいので --no-preload 推奨）
python cli.py serve --no-preload
# CLI個別
python cli.py ingest <URL>            # 撮影（--list urls.txt で一括）
python cli.py embed                   # 未処理を埋め込み（--force で全件）
python cli.py search "上品な青系"      # text→image 検索
python cli.py describe                 # 未処理サイトの雰囲気を言語化（要 .env キー）
python cli.py status                  # 件数表示
```

### 画面でできること
登録／検索／似てる検索／🗑削除／**詳細ビュー（カード画像クリック）**／
**🎨カンプ生成**／**✨雰囲気の言語化**／**🎬動きを録る**／**🖼画像を抜き出す**／**⚙設定**。

### 生成エンジン（Claude / GPT）の切替
- ⚙設定 → エンジンを **Claude / GPT** から選ぶ（選んだ瞬間に自動保存）。
- GPTを使うなら OpenAIキーを入れて **接続テスト**（✅が出れば使える）。
- モデルは `gpt-5.5`（高品質）/ `gpt-5.4`（半額・ほぼ同等）等を設定欄で指定。

---

## 5. ファイル構成

```
起動.bat                ダブルクリックで起動（サーバ＋ブラウザ自動オープン）
cli.py                  CLI入口（ingest/embed/search/serve/status/describe）＋.env読込＋KMP対策
src/
├─ config.py            設定の集中管理（モデルID・撮影条件・パス・プロバイダ・APIキー）
│                        reload()/update_env_file()＝設定画面の保存で.env書込＆即反映
├─ db.py                SQLite（site テーブル）。マイグレーション（vibe_embedding/design_tokens）
├─ ingest.py            Playwright撮影＋bot壁/白紙ゲート＋ライブラリ検出＋トークン抽出＋録画
├─ model.py             SigLIP-2 ラッパー（device自動・CPUはbf16・次元実測）
├─ embed.py             埋め込みパイプライン（常駐embedder受け取り可）
├─ search.py            text→image / image→image / 似た検索 / 雰囲気文ハイブリッド
├─ vibe.py              雰囲気をClaude(Vision)で言語化→SigLIPでテキスト埋め込み
├─ tokens.py            ライブページからデザイントークン抽出（配色・フォント・余白・角丸）
├─ assets.py            ライブページから画像を抜き出してDL（data/assets/<id>/）
├─ camp.py              カンプ生成（手本fullpage踏襲＋トークン＋アニメ＋Claude/GPT切替）
├─ viewer.py            Flask/waitressサーバー（全API・設定・詳細・各種ジョブ）
└─ utils.py             ハッシュID・URL正規化・BLOB変換・ログ
templates/viewer.html   ビューアのUI（検索・登録・設定・詳細モーダル・カンプ生成・進捗バー）
.env / .env.example     APIキー等（.envはgit除外）
data/  screenshots/ videos/ assets/ camps/ design_stock.sqlite （いずれもgit除外）
仕様/                    正式な仕様書
```

---

## 6. 設計上の約束ごと（壊さないこと）

- **元画像（firstview / fullpage）は必ず保存する** → モデルを変えても何度でも再埋め込みできる。
- **モデルは抽象化**：モデルIDは設定値、**次元数はハードコードせず実測**して記録する。
- **`embed_model_name` / `embed_version` / `embed_dim` を持つ** → 古いレコードを判定して再埋め込みできる。
- **類似度は内積（=cosine）**：保存時に L2 正規化済みなので内積でOK。
  - SigLIP は sigmoid 損失でスコアの絶対感覚が CLIP と違う。**閾値は決め打ちせず実データで調整**する。
- **ベクトルDBは使わない**：個人規模（数百〜数千件）は numpy 総当たりで十分速い。
  - 万単位になったら sqlite-vec 等へ。保存と検索を分離してあるので差し替え可能。
- **DB操作とファイル操作を混ぜない**：例）削除は `db.delete_site` が行を消してパスを返し、
  画像ファイルの削除は呼び出し側（viewer）が行う。
- **撮影条件は固定**（viewport 1440×900 / DSF 2 / UA固定）：似た画像検索の精度がブレないように。
- **firstview はスクロール前に撮る**：スクロール連動 fade-in のヒーロー（Notion等）が
  白画面で撮れてしまうのを防ぐため。lazy-load 用のスクロールは fullpage の前に回す。

---

## 7. 既知の問題・ハマりどころ（重要）

### ✅ ⑦ ボタンが全部「Failed to fetch」→ HTML直開き(file://)が原因 → 解決
- **症状**：保存・接続テスト・切替などPOST系が全部「Failed to fetch」。GETは通る。
- **原因**：ツールのHTMLを**ダブルクリックで開いた（file://）**ため、`/api/...`がサーバに届かない。
- **対処**：必ず **`http://127.0.0.1:5000`**（＝`起動.bat`）で開く。さらに開発サーバの
  keep-alive弱さ対策で**waitress（本番品質サーバ）**に変更（`viewer.serve`）。

### ✅ ⑧ 設定/接続テストの作り（GPT/Claude切替）
- `.env` を **最優先**で読む（`cli.py` の `load_dotenv(override=True)`）。古い環境変数に負けない。
- 設定画面の「接続テスト」は**入力欄のキー**を試す（保存前でも検証可）。保存時も自動テスト。
- カンプ生成プロバイダは `config.HtmlGenConfig`（`DESIGN_STOCK_HTML_PROVIDER` / `OPENAI_API_KEY`
  / `DESIGN_STOCK_OPENAI_MODEL`）。`camp.py` の `_call_llm` が Claude/GPT を出し分ける。

### ✅ ⑥ 生成カンプが真っ白／途中で切れる → 解決済み
- **症状**：生成したカンプHTMLを開くと中身が出ない（ヘッダとフッタだけ等）。
- **原因2つ**：
  1. **トークン上限で末尾が途中切れ**（`<script>`が閉じず `</body></html>`も無い）→ JS全滅で
     スクロール出現アニメが動かず真っ白。→ `max_tokens=16000` に増やした。
  2. **スクロール出現アニメ**（`.reveal{opacity:0}`→in-viewで表示）で、開いた瞬間は下が隠れて見える。
- **対処**：`camp._finalize_html()` で「閉じてない`<script>`を捨てる＋必ず全体が見える保険スクリプト＋
  `</body></html>`補完」を必ず通す。カンプは"全体を見て確認する成果物"なので開いたら全表示が正。

### ✅ ① 複数URL登録で「Failed to fetch」→ 解決済み（非同期ジョブ化）
- **症状（旧）**：5件などまとめて登録すると、1リクエストが長時間ブロックして fetch が失敗。
- **対処**：登録を**非同期ジョブ化**した。
  - `POST /api/register` は即 `job_id` を返す → バックグラウンドのスレッドが1件ずつ撮影 →
    最後にまとめて埋め込み。UIは `GET /api/register/status` を1.2秒ごとにポーリングして
    「撮影中 3/5：URL」「ベクトル化中…」と進捗表示。撮れたものから一覧に反映される。
  - 同時に走るジョブは1つだけ（多重起動は 409 で弾く）。
- **残メモ**：ジョブ状態はメモリ上の単一 `_JOB`（プロセス内・1ユーザー想定）。
  複数ユーザーや再起動をまたぐ必要が出たら、ジョブ表をDB化する。

### ⚠ ② セグメンテーション違反（OpenMP二重ロード）
- **症状**：モデル読込時に exit 139 で落ちる（不定期）。
- **原因**：torch同梱の OpenMP(libiomp5md.dll) が numpy/MKL の別 OpenMP と二重ロードされる。
- **対処済み**：`cli.py` の**最先頭**（全importより前）で `KMP_DUPLICATE_LIB_OK=TRUE` を設定。
  - ★ ここを消すと再発する。numpy より前に設定する必要があるので順序を変えないこと。

### ⚠ ⑤ メモリ不足でモデル読込が落ちる（このCPU PC特有）
- **症状**：`ページングファイルが小さすぎる`(OSError 1455) や exit 139。
- **原因**：このPCはページングファイルが約2GBと小さく、**コミット空きが7GB前後**しかない。
  SigLIP2-SO400M は fp32 で約4.3GB、読込ピークがこの上限を超える。
- **対処済み**：**CPUでは bfloat16 で読み込む**（`model.py`・約2.2GBに半減）。GPUでは float16。
- **それでも落ちるとき**：クラッシュした残プロセスがコミットを握っていることがある。
  我々のvenv由来の python ゾンビを kill するとコミットが空いて起動できる
  （`ExecutablePath -like '*86_*venv*'` で絞って Stop-Process）。
- GPU PC に移ればこの制約は消える（VRAM 8GB で fp16 が余裕）。
- **当面の運用**：他アプリでコミットが逼迫していると `serve` 起動時のモデル先読みで落ちる。
  その場合は `python cli.py serve --no-preload`（モデルは初回検索時に読む）で起動できる。

### 🔑 APIキー（雰囲気描写ハイブリッド用）
- `ANTHROPIC_API_KEY` を **`.env`** に置く（`.env.example` を参照）。`.gitignore`で除外済み。
- 取得：https://console.anthropic.com/ → API Keys。コードやログに出さない。

### ⚠ ③ transformers 5.x の API 変更
- `get_image_features` / `get_text_features` は**テンソルではなくオブジェクト**を返す。
  正しい埋め込みは `.pooler_output`（次元1152）。`model.py` の `_postprocess` で吸収済み。

### ⚠ ④ JS描画が重いSPA・同意バナー
- Notion等は描画前に撮ると白画面 → `networkidle` 待ち＋撮影順の工夫で対処済み。
- 同意バナーの自動クローズは best-effort。文言が合わなければ
  `src/ingest.py` の `_CONSENT_TEXTS` に手で足す（汎用「OK」は誤クリックの元なので入れない）。

### ✅ ⑨ 右クリック直後にドラッグを掴んでしまい、文字選択（下線/マーカー）ができない → 解決
- **症状**：下線やマーカーを一度引くと、その要素にカーソルを合わせただけで十字の移動カーソルになり、
  もう一度文字を選択できず消せなくなる。
- **原因**：右クリックした瞬間に自動でドラッグモードON（`setDragOn`）にする仕様だったため、
  以降のmousedownがすべて「要素移動」に奪われ、文字のドラッグ選択ができなくなっていた。
- **対処済み**：右クリックだけでは動かさず、「🖱 掴んで動かす」ボタン／Alt+ドラッグでのみ移動するよう変更。

### ✅ ⑩ セクション等の高さ変更で歪む（scaleとmin-heightの取り違え）
- **症状**：右クリックの「⬍高さ」調整のつもりで「⇕縦を縮め」を使うと、中身が潰れて見た目が崩れる。
- **原因**：「⇕縦に長く/縮め」はCSSの`scale`（=歪む）、「＋高く/－低く」は`min-height`（=歪まない）で、
  別物なのに紛らわしかった。
- **対処**：歪んだ`scale`はリセット（`1 1`に戻す）し、高さは`padding`で調整するのが正解。
  歪ませたい場面が無ければ「⇕縦に長く/縮め」自体は使わない。

### ⚠ ⑪ ゾンビサーバー（port 5000）でコード修正が反映されない
- **症状**：Python(`viewer.py`/`camp.py`)を直したのに挙動が変わらない／古い文言のまま。
- **原因**：`cli.py serve`の**古いプロセスがport 5000を握ったまま**（venv版とsystem Python版が
  両方生き残っていることがある）。Windowsは複数プロセスが同じportをLISTENINGできてしまう。
- **対処**：`netstat -ano`や`Get-NetTCPConnection -LocalPort 5000`でPIDを特定→
  `Get-CimInstance Win32_Process -Filter "ProcessId=<pid>"`でコマンドラインを確認→
  `Stop-Process -Id <pid> -Force`で全部止めてから`起動.bat`で1個だけ起動し直す。
  （詳細は auto-memory の `zombie-server-port5000.md` にも記録）

### ✅ ⑫ ブラウザ拡張機能(Glasp)のUI断片が保存のたびに紛れ込み肥大化 → 解決
- **症状**：保存を繰り返すうちにカンプHTMLがどんどん重くなる（1ファイルで708個・約140KB分の肥大化）。
- **原因**：保存(`cleanHtml`)は「今のブラウザ画面のDOM」をそのまま保存する仕組みのため、
  Glasp等の拡張機能がページに注入したUI部品も一緒に保存され、保存するたびに積み重なっていた。
- **対処済み**：`cleanHtml`で`[class*="glasp-extension"]`を保存前に自動除去するよう修正。

### ✅ ⑬ 🖍マーカーが保存後「最初から引かれた状態」になる（--hlwリセット漏れ） → 解決
- **症状**：マーカーが伸びるアニメを一度見たあとに保存すると、次回開いた時にアニメせず
  最初から100%引かれた状態で表示される。
- **原因**：保存時に「表示中」の目印クラス(`fxa_in`)は外していたが、伸び具合を持つ
  カスタムプロパティ`--hlw`（0〜100）のリセットが漏れていた。
- **対処済み**：`cleanHtml`で`.fxa_hl`要素の`--hlw`を必ず`0`に戻すよう修正。

### ✅ ⑭ 特定のカンプで出現アニメ（3D回転等）を付けても一切動かない（SHOWクラス焼き込み） → 解決（2026-07-11）
- **症状**：右クリック→動きを付けても、そのカンプだけ全く動かない（camp_20260710_235238で発生）。
- **原因**：⑬と同じ家系。カンプ内の保険スクリプト（`camp.py`の`_REVIEW_FALLBACK`）はスクロールで
  見えた要素に「見せるクラス」16種（`in show inview aos-animate…`）をまとめて付ける。その状態で
  💾保存するとDOM丸ごと保存方式のため**クラスごと焼き込まれる**。ページ固有CSSに
  `.inview{opacity:1!important;transform:none!important}`があると、fxaの出現アニメが永久に負ける。
- **対処済み**（`viewer.py`）：
  1. `cleanHtml`（保存）：reveal系要素からSHOWクラス16種を除去＝開き直せば保険がまた付けるので見た目は不変。
  2. `applyBake`（動きを付ける瞬間）：対象要素のSHOWクラスを剥がして主導権を取り戻す＝**汚染済みの既存ファイルも右クリックで動きを付け直すだけで直る**。

---

## 8. 環境・前提

- **言語**：Python（SigLIP-2 の HuggingFace エコシステムに合わせる）。
- **新PC（GPUあり予定）**：RTX 3070Ti(8GB) 等。今のPCは **GPUなし → CPUで稼働中**。
  - device は **自動判定**（CUDAあればGPU・なければCPU）。コード変更なしでGPUに移れる。
  - 移行手順：venv作り直し → `pip install torch --index-url .../cu121` に変えるだけ。
- **現在のvenv**：Python 3.12.7 / torch CPU版 / transformers / playwright / flask。
  - ※ Python 3.13 はこのPCで壊れたショートカット状態。3.12 を使う。
- **モデル**：`google/siglip2-so400m-patch14-384`（出力1152次元・多言語・Apache2.0）。
  - 一度DL済みなら**ローカルキャッシュからオフライン読込**（毎回のHub通信を省いて速い）。

---

## 9. ロードマップ（今後）

- **Phase 2.x（次の課題）**
  - [x] 登録の**非同期ジョブ化**（上記①の解消・進捗表示）← 完了
  - [x] **ホバーで全体プレビュー**（カードにfullpageを流し見・サイトに飛ばず雰囲気を掴む）← 完了
  - [x] **取り込みゲート**（bot壁・白紙を検出して保存しない＝ゴミがindexに入らない）← 完了
  - [ ] **検索プロンプトのチューニング**（SigLIPテキストの整形）。今回はメモリ制約で
        A/B検証を見送り。**GPU PCに移ってから**実データで比較して採用判断する。
  - [ ] bot壁サイトを**非headlessで撮り直す**オプション（今は検出して弾くだけ）
  - [x] 一覧の**並び替え／絞り込み**（撮影日・URL名・動画有無・アニメライブラリ）← 完了
        （2026-07-07・クライアント側で実装。`/api/sites`等は変えず取得済みhitsを絞るだけ）
  - [ ] 撮り直し（re-capture）ボタン（サイトはリニューアルするので）
- **Phase 3（精度・メタデータ）**
  - [x] **雰囲気描写文ハイブリッド** ← 完了
        Claude(Vision)がスクショの雰囲気を1段落で説明 → その文もSigLIPでベクトル化 →
        検索は「画像スコア＋雰囲気文スコア」を候補内で標準化して重み付き合算（`src/vibe.py`）。
        これで「高級感／上品／信頼感」など抽象的な言葉でも当たる。
        使い方：UIの「✨雰囲気」ボタン、または `python cli.py describe`。
        APIキーは `.env` の `ANTHROPIC_API_KEY`（`config.VibeConfig`）。重みは `VibeConfig.weight_*`。
  - [ ] 自動タグ付け（語彙は事前固定）
  - [x] **デザイントークン抽出**（配色・フォント・余白・角丸・横並び数）← 完了
        ライブページから実数値を抽出（`src/tokens.py`）→ `design_tokens`(JSON)に保存 →
        カンプ生成でClaudeに**具体的に**渡す。これで初めて出力に"そのサイトらしさ"が乗る。
        カードに配色スワッチ表示、「🎨ベースに生成」で1サイトを主役に指定できる。
        ★効果実証：ichizenbooks(青×黄)を主役に生成→配色がそのまま乗った「ヨムタビ」が出来た。
  - [x] アニメのオンデマンド取得：**スクロール録画(webm)**＋ホバーで動画再生 ← 完了
        （カードの「🎬動きを録る」で1サイトずつ録画。`animation_video_path`に保存）
  - [x] **アニメ系ライブラリ名の検出**（GSAP/Lenis/Swiper/Lottie/AOS等）← 完了
        撮影/録画時に検出して `animation_libs` に保存、カードにタグ表示。
        目的：カンプ生成時にAIへ「あのGSAPみたいな演出」と言葉で頼む手がかり。
- **Phase 4（カンプ生成 = デザインRAG）**
  - [x] 手本fullpageの骨格を踏襲＋トークンで配色を寄せた**叩き台HTML生成** ← 完了
  - [x] アニメは「スクロール出現」をCSS/JSで付与（壊れても見える保険込み）← 完了
  - [x] **スクショ→HTML** 形式（SVGは不採用）／**Claude/GPT切替** ← 完了
  - [x] **詳細ビュー**（サンプルを1画面で見る）／**画像の抜き出し** ← 完了

### ★ これからやること（次の山・2026-07-07 優先順位づけ）
ユーザーの狙い＝**登録HPを"部品"として、混ぜて、別業種の叩き台を作る**。
（例：「Aの見た目に、Bのアニメを入れて、就労支援事業所版を作って」）

**優先度順（今すぐ着手できるか判定済み）**：

1. **一覧の並び替え・絞り込み** ← 完了（2026-07-07）
   - 撮影日新しい/古い順・URL名前順・動画ありのみ・アニメライブラリ絞り込みを実装済み
     （`templates/viewer.html` の `#sort_sel`/`#filter_video`/`#filter_lib`・`setHits`/`applyAndRender`）。
2. **③アニメーションの抜き出し** ← 完了（2026-07-07）
   - ★判明した事実：**バックエンド（`src/anim.py`の`extract_animations`/`anim_to_prompt`、
     `/api/extract_anim`、DBの`animation_snippets`カラム、`camp.py`の`_anim_ref_block`での
     mix&matchプロンプト組み込み）は本セッション開始前から実装済みだった**。CSSも
     `.detail_anim`/`.anim_stage_grid`等、詳細ビュー用に準備済みだったが、
     **肝心の「詳細ビューにボタンを置いて呼び出す」配線だけが未接続**で、機能として使えない状態だった。
     （ロードマップの「未着手」表記が実態とズレていた＝コードを見て判明・メモリよりコード優先の教訓）
   - 今回追加したのはこの配線のみ：詳細ビューに3つ目のタブ「🎞 アニメ素材」を追加
     （`#detail_tab_anim`/`#detail_pane_anim`）。「🎞 アニメを抜き出す」ボタン→
     `/api/extract_anim`→ポーリングで完了待ち→`renderAnim()`が
     タグ(件数)・実際に再生されるデモ箱(`@keyframes`を実サイトのfirstview画像に重ねてループ再生・
     ホイールでコマ送り)・Lottie再生(`<lottie-player>`)・生CSSコード片を表示。
   - GSAP等のJS系は仕様どおり抜けない→録画動画＋ライブラリタグ＋言葉で参照、のまま。
   - ★追加改善（同日・ユーザー指摘で発覚）：実際に抜き出すと`fadeIn`/`slideIn`/`swiper-*`など
     **Swiper/animate.css等の付属品（そのサイト独自ではない汎用エフェクト）ばかり混ざる**問題があった。
     `is_generic_keyframe()`（正規表現の簡易判定）で汎用名を検出し、**独自を先頭・汎用を後ろに自動で並べ替え**。
     `anim_to_prompt()`もAIに見せる代表4個を**独自優先**に変更（汎用ばかりで見本枠が埋まるのを防止＝
     mix&match生成の質に直結する実質的な改善）。詳細ビューでも✨マーク＋区切り線で独自/汎用を視覚的に分離。
3. **カンプ生成を mix & match に拡張** ← 実は完了済み（本セッションで判明）
   - `camp_base`/`camp_anim`のサムネ選択UI、`/api/generate_camp`への`anim_id`受け渡し、
     `camp.py`の`_anim_ref_block`によるプロンプト組み込みまで、既に一通り繋がっている。
   - ②の抜き出しボタンが今回使えるようになったことで、**この機能が実質的に完成**した
     （今までは`animation_snippets`を溜める手段がなく、機能はあっても実質使えなかった）。

**保留・後回し（今すぐは着手しない）**：
- **疑似要素(`::before/::after`)の直接編集** → ⚠️半分できない。DOMに実体が無いのが根本原因。
  本物のdiv要素に作り替える変換をすれば掴めるが元CSSが崩れるリスクあり。優先度低め。
- **検索クエリのチューニング** → ❌今は厳しい。このPCがGPUなし・コミット空き7GB前後の制約で
  A/B検証がやりづらい。**新PC(GPU)移行後**に実データで比較して採用判断する。

その他：仕上げは外部（**Codex**等）で。叩き台に抜き出した画像/アニメを仮置き→そこで磨く前提。
古いサイトのトークン再取得（全カードに配色が入る）も未対応のまま残っている。

---

## 9.5 カンプ生成の品質を決める要点（実地で分かったこと）

「登録サイトが効いてない／汎用AIっぽい」を避けるための肝。順に効く：

1. **レイアウトの骨格は"全体スクショ(fullpage)"を渡して踏襲させる**（最重要・決定打）。
   ヒーロー1枚＋色だけだと、Claudeは汎用レイアウトを自作する＝"らしさ"が出ない。
   主役サイトの fullpage を渡し「セクションの種類・順番・構図・密度を忠実に再現」と指示する。
   → `camp.py` は base_site の fullpage を「レイアウトの手本」として渡している。
2. **デザイントークン（実際の配色・フォント・余白・角丸）を具体的に渡す**（`tokens.py`）。
3. **画像は手描きさせない**。CSS/SVGで人物や風景を描かせると稚拙になる。
   → LoremFlickr 等のダミー写真を使う（`https://loremflickr.com/{w}/{h}/{keyword}?lock={n}`）。
4. **AIっぽい定番（横3カラム均等・左右対称の繰り返し）を明示で禁止**。
5. **中身（コピー・画像・ロゴ）は全部自前に差し替える**＝「参照→作り直し」（著作権セーフ）。

実証：ichizenbooks を主役に「ヨムトモ」を生成 → 縦書き・serifラベル・青CTA帯・Step01-03・
FAQ・Voice・末尾の再CTA まで**10セクションの骨格が一致**。色も青×黄。中身は全部オリジナル。

## 9.7 運用（家↔会社の同期・Git）

- リポジトリ：`git@github.com:yoshida55/desing_kanpu_create.git`（SSH）。
- **コードと生成カンプ(HTML)をGitに上げる**。`venv/`・`data/`のscreenshots/videos/assets/sqlite・
  `.env`は`.gitignore`で除外（**.envにはAPIキーが入るので絶対に上げない**）。
  ただし**`data/camps/`（生成カンプHTML）は2026-07-06以降Git管理に変更**＝軽いテキストで
  単体完結なので、一斉バッチでそのまま家↔会社に共有される。
- **同期は中央の「一斉バッチ」を使う**（プロジェクト個別バッチは作らない）：
  - `D:\99_AIソフト\85_bat(会社・家同期)\commit_all_実行.bat`（add→commit→push を全リポ一斉）
  - `D:\99_AIソフト\85_bat(会社・家同期)\pull_all.bat`（全リポ一斉 pull）
  - これは `D:\99_AIソフト` 配下の **.git＋リモートを持つフォルダを自動で拾う**ので、
    この 86_ は**git化＆リモート設定済み＝自動で対象**（手動登録不要）。
  - 自動対象から外したいときは、そのフォルダに `.manual_commit_only` を置く。
- 別PC（会社）で初めて使うとき：
  1. `git clone git@github.com:yoshida55/desing_kanpu_create.git`
  2. README の手順で venv 構築（torch等インストール／playwright install chromium）
  3. `.env.example` をコピーして `.env` を作り、APIキーを入れる（または⚙設定画面から）
  4. `起動.bat` で起動
- ※ DB・スクショ・録画・抜き出し画像は**このリポジトリのGitでは**同期されない（容量大のため）。
  **カンプHTML(`data/camps/`)だけは例外で同期される**（軽い＋成果物として共有したいため）。

### 9.7.1 サイトライブラリ（DB・スクショ・録画・画像）をナレッジ経由で家↔会社共有（2026-07-10〜）

会社PCで「登録済み：0件」になる問題（`data/`はGit対象外＝各PCに別データ）への対策として、
保存先をナレッジフォルダ（`03_knowledge`/`50_knowledge`、こちらは別のGitリポジトリとして
既に家↔会社で同期済み）の中へ向けられるようにした。

- `src/config.py` に **`DESIGN_STOCK_LIBRARY_DIR`**（環境変数）を追加。
  設定すると `DB_PATH` / `SCREENSHOT_DIR` / `VIDEO_DIR` / `ASSET_DIR` / `ANIM_DIR` が
  そこに向く（未設定なら従来通り `data/` 配下）。`MOTION_DIR`/`UPLOAD_DIR`/`CAMP_DIR` は対象外
  （motionは動画から再生成できる中間データ、campsは元々このリポジトリで同期済みのため）。
- **会社PC設定済み**：`.env`（gitignore対象・このPC限定のファイル）に以下を設定し、
  フォルダ `C:\Users\guest04\Desktop\高橋研三\03_knowledge\ツール\desing_kanpu_create_data\` も作成済み。
  ```
  DESIGN_STOCK_LIBRARY_DIR=C:\Users\guest04\Desktop\高橋研三\03_knowledge\ツール\desing_kanpu_create_data
  ```
- **🏠 自宅PCでやること（未実施・次回自宅で作業するときに）**：
  1. `.env` に以下を追記する（`D:\50_knowledge` 配下に同名フォルダを作る）：
     ```
     DESIGN_STOCK_LIBRARY_DIR=D:\50_knowledge\ツール\desing_kanpu_create_data
     ```
  2. 今まで使っていた `data\design_stock.sqlite` と `data\screenshots\` `data\videos\`
     `data\assets\` `data\anim\` の中身を、上の新フォルダへ**移動**する
     （中身がある状態でツールを起動すれば、そのまま登録済みサイトとして読み込まれる）。
  3. ナレッジフォルダ（`D:\50_knowledge`）側の同期手段（Git push等・普段どおりのやり方）で
     会社PC側に反映する。会社PCは `03_knowledge` を pull すれば自動的にこのツールにも反映される
     （`.env`の設定は変更不要・パスは既に会社PC用に設定済みのため）。
  4. ⚠注意：スクショ・録画は画像/動画なのでナレッジのGitリポジトリの容量が増える。
     サイト登録数が増えてきて重く感じたら、この方式をやめて外部ストレージ同期に切り替える手もある
     （そのときは `DESIGN_STOCK_LIBRARY_DIR` の値を変えるだけで移行できる）。

- ✅ **移動後にデザイン一覧の画像が404になるバグを修正済み（2026-07-10・自宅PC移行完了）**：
  DBのパスは共有のため相対（`data\screenshots\…`）のまま。旧コードはそれを
  `PROJECT_ROOT` 直下でしか探さず、移動後は全スクショ404＋新規登録も
  `relative_to(PROJECT_ROOT)` の ValueError でクラッシュする状態だった。
  → `config.resolve_data_path()`（読み：先頭の `data\` を LIBRARY_DIR に読み替えて探す）と
  `config.data_rel_path()`（書き：LIBRARY_DIR配下でも従来の `data\…` 形式で記録）を新設し、
  viewer/camp/embed/vibe/motion/ingest/assets/anim の読み書き全箇所を差し替えた。
  DBの形式は変わらないので会社PCでもそのまま動く。

## 10. 作業するときの心得（このプロジェクト固有）

- まず **本ファイル → 仕様書** の順で現在地を把握する。
- 設定は `src/config.py` に集約。**数値や文字列をコード中に直書きしない**。
- 検索精度の検証は「このクエリでこのサイトが上位に来てほしい」を**先に5〜10個メモ**し、
  クエリやモデルを変えたとき**差分で良し悪しを判定**する（「なんとなく良さそう」で進めない）。
- 著作権：個人のリファレンス収集はOK。納品に流すときは「参照→作り直し」を徹底。
  robots.txt・利用規約・レート制限を尊重する。
