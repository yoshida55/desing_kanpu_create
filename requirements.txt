# デザイン参照ストック & 曖昧検索システム（Phase 1）

# 撮影（Playwright / Chromium）
playwright>=1.44

# 埋め込み（SigLIP-2 = HuggingFace transformers）
transformers>=4.45
# torch は環境(GPU/CPU)でインストール方法が違うので README を参照
torch>=2.2
pillow>=10.0

# 検索（numpy総当たり）
numpy>=1.26

# ローカルビューア（Phase 2：検索ボックス付きUI）
flask>=3.0
waitress>=3.0

# .env 読み込み
python-dotenv>=1.0

# 画像の背景除去（rembg = onnxruntime + U2-Net・ローカル/無料・透過PNG）
rembg>=2.0
onnxruntime>=1.17

# カンプ生成・修正（雰囲気描写＝Claude / カンプ生成＝Claude・GPT・DeepSeek等）
anthropic>=0.40
openai>=1.50
