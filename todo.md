# todo（2026-07-30 更新・🏠自宅PC）

## すぐやる
- [ ] 今日の修正を反映して動作確認：`起動.bat` で再起動 → `Ctrl+Shift+R`
      → ✂背景の切り抜きを実際のカンプで1回押して確かめる（コマンドでは通っている）
      → カンプを開いて、白い待ちが無くすぐ中身が出るか見る
- [ ] コミットして上げる（`commit_all_実行.bat`）※ bgremove.py / viewer.py / CLAUDE.md / todo.md が未コミット

## ✅ カンプを開くと2〜3秒真っ白 → 直した（2026-07-30 夜）

正体は🎬オープニングの幕。幕の色がほぼ白（`#eafff6→#eef4ff→#ffffff`）で、その間ページの動きを
止めているので「演出」ではなく「白い待ち時間」に見えていた。

実測（あしたばのカンプ・1440×900）：

| | 幕が出ている間 | 中身が出そろう |
|---|---|---|
| 前 | 591ms → 2822ms | **3.7秒** |
| 後 | 出さない | **1.1秒** |

- 編集中（ツールで開いた時）だけ幕を流さない。**保存版・単体HTMLでは今までどおり流れる**
- 幕は消していない（`display:none` にするだけ）＝👁 → ▶ で本番と同じ動きを確認できる
- 幕そのものが要らないカンプは 🗑 オープニングを外す

### もう1件：幕が無いカンプが「永久に真っ白」だった（同日・別原因）

幕を消したときに「待て」の合図（`__op_early`）だけ残っているカンプがあった。外す役（`__op_run`）が
いないので待ちが解除されず、**10秒たっても中身が出ない**（`_bk20260730_071505` で実測 0.48%）。

- 対処：幕が無いのに待機スクリプトだけ残っていたら丸ごと外す → 永久に白 → 5秒で表示になった
- [ ] **壊れている既存カンプは、開いて💾保存し直すと単体でも直る**（保存するまでファイル自体は壊れたまま）

### さらにもう1件：再生されなかった出現アニメを起こすようにした

`.fxa_pre` は監視が `.fxa_in` を付けて初めて見える。取りこぼすと保険の掃除も「fxaは触らない」約束
なので誰も助けず、ヘッダー等が永久に透明のままだった（実測：11個中3個しか再生されず）。

- 対処：保険の掃除の先頭で、画面内の `.fxa_pre:not(.fxa_in)` に `.fxa_in` を付ける（＝遅れて再生）
- ⏳遅らせはページを開いた時から数える（まるまる待つと逆に白い時間が延びた）

### 実際に踏んだ形（作業中の `_bk20260730_071505`）

ヒーローの🖼スライドショー（1911×948）が `<header>` の**中**に入っていて、そのヘッダーが
再生されず opacity:0 → **中身ごと丸ごと見えない**＝ファーストビューが真っ白に見えていた。
スライドショー自体は正常（3枚・2秒間隔で回っていた）。上の修正で4秒後に全部出るのを確認。

- ⚠親が透明なだけなので、子（画像）を調べると opacity:1 で「異常なし」に見える。
  出ない要素は**親をたどって opacity を見る**のが早い
- [ ] ヒーローが `<header>` の中にあるのは構造としては不自然。気になるなら
      🎯レイヤー選択で `<section>` 側へ出しておくと、ヘッダーの動きに巻き込まれなくなる

## ✂ 背景の切り抜き → ✅ C案で解決（2026-07-30 夜）

**rembg を使わず onnxruntime で u2net.onnx を直接動かす方式に変えた。追加インストールなし。**

実測（`data/uploads` の画像で確認）：

| 画像 | 結果 |
|---|---|
| 鳥＋ハートの絵（平らな白背景） | ✅ 1.0秒で背景が完全に透明・輪郭もきれい（透明90.0%／四隅alpha=0） |
| 街と草原の風景イラスト | ⛔ ほぼ全部消える → 手前で止めて理由を出すようにした（下記） |

### なぜ止まっていたか（戻さないための記録）
rembg は import しただけで pymatting → numba を読む。numba の .pyd は署名が無いので、
この自宅PCの**スマートアプリコントロール（オン・強制）**に弾かれて必ず落ちていた。
numba が要るのはフチの仕上げだけで、本体（onnxruntime＋u2net）は両方とも正常。
- ⚠ **ここを rembg に戻すと自宅PCで再発する。** OS側を切る解決は採らない
  （スマートアプリコントロールは一度オフにすると Windows再インストールしないと戻せない＝片道切符。
  しかもクリーンインストール限定機能なので🏢会社PCではまずオフ＝あちらでは不要な対処。
  PCごとに挙動が変わるので却下した）

### 分かった限界（仕様として受け入れる）
u2net は「主役を1つ選ぶ」AIなので、**風景まるごとの絵・柄・背景素材は主役が無く丸ごと透明になる**。
rembg でも同じ結果。押した人が「絵が消えた」と思わないよう、
残る面積が2%未満なら保存せず「主役が見つかりません」と理由を返すようにした。

## ✅ 追加した4つ（2026-07-30 夜・要望）

| | 内容 | 実測 |
|---|---|---|
| 🧯 | 画面の3割以上を覆う要素の⏳遅らせ(600ms以上)は、開いた時に自動で外す | 3.6秒の遅らせ→自動解除・opacity 1 |
| 🖼 | スライドショーを**左クリック**すると写真の選び直し画面が出る | 「全3枚。緑の番号が今入っている画像」表示 |
| 🖼 | スライドショーは常に `z-index:0`＝一番奥。あとから足した文字が裏に潜らない | computed z-index = 0 |
| 🕊 | 紙飛行機（空飛ぶルート）も ⏳一覧に並び、順番・速さを変えられる | 一覧13行中2件・速さ4000→1500・遅らせ2200 反映 |

| ⏳ | 一覧が「このセクション」だけで1件しか出なかった → 自動でページ全体に広げる＋切替ボタン | 1件 → 13件 |
| 🔍 | 画像を選ぶパネルを大きく（箱 640→1120px・サムネ 高さ80→140px） | 実測 1120×792・サムネ206×140 |

- ⚠ 遅らせの自動解除は「大きい物」だけが対象。小さい飾りの遅らせは今までどおり残る
- ⚠ 🕊の速さは「飛びきるまでの所要時間(ms)」。他のアニメの速さとは持ち方が違う（JSONの中）
- ⚠ パネルを大きくしたのは画像・セクションを選ぶ時だけ。⭐お気に入りなど文字だけの一覧は元の幅

## ✅ 2026-07-31 に直したもの（グループ／スライドショー）

| | 内容 | 実測 |
|---|---|---|
| 🧩 | **Ctrl+クリックの複数選択が未実装だった**ので追加（これが「グループ化できない」の正体） | 2個選択→Ctrl+G→同じ印が付く |
| 🧩 | グループはちゃんと一緒に動いていた（移動側にバグは無し） | 2要素とも同じ(108,54)移動 |
| 🧩 | グループの1つに動きを付けると仲間にも同じ動きが付く | A/B とも `fxa_pre fxa_xl` |
| ✨ | 入れ子（文字と親の箱）を一緒に選んだ時は**外側だけ**に付ける＝「動きが固まる」の解消 | — |
| 🖼 | スライドショーのパネルは**ダブルクリック**で開く（1回クリックから変更） | 1回=開かない／2回=開く |
| 🖼 | **1枚目から選び直せる**（前は2枚目以降だけ） | 緑の番号が 1,2,3 |

## 手で直すもの
- [ ] 1450pxで作ってしまったスライドショーの箱を ■ハンドルで縮める
      （新しく作る分は写真の実寸になるので、この1件だけ）

## 未着手のアイデア
- [ ] スライドショーを1枚ずつ違う大きさにする（今は3枚まとめて変わる）
- [ ] 切り抜きの精度が足りない写真用に、人物特化モデル（u2net_human_seg）へ切り替える口を画面に出す
      （今は環境変数 `DESIGN_STOCK_BGREMOVE_MODEL` で差し替えられる状態まで）

---

## 今日直したこと（メモ）
- 🧲 位置をそろえる（整列・等間隔）／掴む枠を見た目に合わせる
- 右クリックメニューを、いじりたい要素にかぶらない場所に出す
- スライドショー：白い空白・同じ画像しか出ない・幅が親いっぱい・押し直すと無言で解除・
  見えている1枚が掴めない・■で3枚まとめてサイズ変更、を修正
- 🗑 オープニングを外すボタンを追加（👁は編集中だけ隠す物で、保存すると戻っていた）
- 📌貼り付けの z-index 暴走（2147480000）を修正＋開いた時に自動で直す
- ✂背景の切り抜きを rembg 経由から onnxruntime 直叩きに変更（上記）。
  あわせて画面の文言を実測に合わせた（「10〜30秒」→「数秒」・「AIなし」→「このPCの中だけで処理」）
